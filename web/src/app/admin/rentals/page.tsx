import RentalsList from '@/components/admin/RentalsList';

export const metadata = { title: 'Admin · Rentals — Eastern Pro Rentals' };

export default function AdminRentalsPage() {
  return <RentalsList />;
}
