import Dispatch from '@/components/admin/Dispatch';

export const metadata = { title: 'Admin · Dispatch — Eastern Pro Rentals' };

export default function AdminDispatchPage() {
  return <Dispatch />;
}
