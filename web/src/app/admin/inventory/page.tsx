import InventoryManager from '@/components/admin/InventoryManager';

export const metadata = { title: 'Admin · Inventory — Eastern Rentals' };

export default function AdminInventoryPage() {
  return <InventoryManager />;
}
